spring:
  application:
    name: registry

server:
  port: 10000

security:
  basic:
    enabled: true
  user:
    name: tr
    password: 123456

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://tr:123456@localhost:${server.port}/eureka

logging:
  path: ../../logs

