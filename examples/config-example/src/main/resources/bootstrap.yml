spring:
  application:
    name: config-example
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      username: config
      password: 123456

server:
  port: 7000

eureka:
  client:
    serviceUrl:
      defaultZone:  http://registry:123456@localhost:10000/eureka