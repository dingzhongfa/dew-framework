spring:
  application:
    name: springcloudtest-config-example
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      username: tr
      password: 123456
      label: 1.0

server:
  port: 7002

logging:
  path: ../../logs/ce2

eureka:
  client:
    serviceUrl:
      defaultZone:  http://tr:123456@localhost:10000/eureka