spring:
  application:
    name: cluster-example
  redis:
    host: localhost
    port: 6379
    database: 0
#  hazelcast:
#    addresses: ["127.0.0.1"]

dew:
  cluster:
    cache: redis
    dist: redis # 可选 hazelcast
    mq: redis # 可选 hazelcast

logging:
  path: ./logs